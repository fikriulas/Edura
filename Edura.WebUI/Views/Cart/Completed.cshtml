@model OrderComplateModel
@{
    ViewData["Title"] = "Index";
}

<!-- CONTENT START -->
<section id="content">

    <!-- shop section start -->
    <section id="cart" class="container">

        <!-- row start -->
        <div class="row">

            <!-- cart contents start -->
            <div class="col-md-8">
                <div class="table-responsive">
                    <table class="table table-hover">
                        <thead>
                            <tr>
                                <th></th>
                                <th>Product</th>
                                <th>Price</th>
                                <th class="qtycolumn">Quantity</th>
                                <th>Total</th>
                                <th></th>
                            </tr>
                        </thead>

                        <tbody>
                            @foreach (var item in Model.OrderLine)
                            {
                                <tr>
                                    <td><img alt="" class="img-responsive product-thumb" src="/edura/images/products/"></td>
                                    <td><a href="#">@item.Product.Name</a></td>
                                    <td></td>
                                    <td>
                                        <div class="form-group">
                                            <input type="number" class="form-control" value="@item.Quantity" min="1" step="1">
                                        </div>
                                    </td>
                                    <td>@((item.Quantity * item.Product.Price).ToString("C"))</td>
                                    <td>
                                        <form asp-action="RemoveFromCart" asp-controller="Cart" method="post">
                                            <input type="hidden" name="ProductId" value="" />
                                            <button type="submit"><i class="fa fa-times fa-fw"></i></button>
                                        </form>
                                    </td>
                                </tr>
                            }

                        </tbody>

                    </table>

                </div>

                <div class="text-right">
                    <a href="#" class="btn btn-default"><i class="fa fa-refresh fa-fw"></i> Update Cart</i></a>
                </div>
                <table class="table table-hover">
                    <thead>
                        <tr>
                            <th>Adres Başlığı</th>
                            <th>Adres</th>
                            <th>Semt</th>
                            <th>Şehir</th>
                            <th>Tel</th>
                        </tr>
                    </thead>
                    <tbody>
                        <tr>
                            <td>@Model.Order.AdresBasligi</td>
                            <td>@Model.Order.Adres</td>
                            <td>@Model.Order.Semt</td>
                            <td>@Model.Order.Sehir</td>
                            <td>@Model.Order.Telefon</td>
                        </tr>
                    <tbody>
                </table>
            </div>
            <!-- cart contents end -->
            <!-- cart total start -->
            <div class="col-md-4">
                <div class="text-left">
                    <h4>CART <b>TOTALS</b></h4>
                </div>

                <table class="table">
                    <tr>
                        <th>Cart Subtotal</th>
                        <td>@Model.Order.Total.ToString("C")</td>
                    </tr>
                    <tr>
                        <th>Shipping</th>
                        <td>Free</td>
                    </tr>
                    <tr>
                        <th>Order Total</th>
                        <td class="ordertotal">@Model.Order.Total.ToString("C")</td>
                    </tr>
                </table>

                <div class="text-right">
                    <a asp-controller="Cart" asp-action="Checkout" class="btn btn-primary btn-lg"> Checkout <i class="fa fa-arrow-circle-right fa-fw"></i></a>
                </div>

            </div>
            <!-- cart total end -->


        </div>
        <!-- row end -->

    </section>
    <!-- shop section end -->

</section>
<!-- CONTENT END -->
